import { RoutePlaceholder } from '@/src/components/common/route-placeholder';

export default function CalendarScreen() {
  return <RoutePlaceholder title="캘린더" description="조회 전용 캘린더 화면은 후속 UI Issue에서 구현합니다." />;
}
