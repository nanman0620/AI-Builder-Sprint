import { RoutePlaceholder } from '@/src/components/common/route-placeholder';

export default function PlanManagementScreen() {
  return (
    <RoutePlaceholder
      title="계획관리"
      description="NEW_CYCLE_ENTRY부터 EXECUTION_FAILED까지 9개 screenMode는 이 Route 하나에서 후속 UI Issue가 구현합니다."
    />
  );
}
