import { RoutePlaceholder } from '@/src/components/common/route-placeholder';

export default function SettingsScreen() {
  return (
    <RoutePlaceholder
      title="설정"
      description="프로필·로그아웃·회원탈퇴 메뉴는 후속 설정 Issue에서 구현합니다."
    />
  );
}
